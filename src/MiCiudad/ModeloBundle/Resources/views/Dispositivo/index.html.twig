<h1>Dispositivo list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Dispositivo</th>
            <th>Version</th>
            <th>Detalle</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('dispositivo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.dispositivo }}</td>
            <td>{{ entity.version }}</td>
            <td>{{ entity.detalle }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('dispositivo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('dispositivo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('dispositivo_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
